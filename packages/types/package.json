{
  "name": "@anomix/types",
  "private": true,
  "description": "common types for Anomix-Network",
  "version": "0.1.0",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "license": "MIT",
  "scripts": {
    "lib:dev": "npm run lib:build && concurrently \"tsc --build tsconfig.json --watch\" \"tsc-alias -w\" \"tsc --build tsconfig.es.json --watch\" \"tsc-alias -w -p tsconfig.es.json\"",
    "lib:build": "tsc --build tsconfig.json && tsc-alias && tsc --build tsconfig.es.json && tsc-alias -p tsconfig.es.json",
    "lib:clean": "rimraf ./dist ./dist_es ./tsconfig.es.tsbuildinfo ./tsconfig.tsbuildinfo",
    "lib:lint": "eslint \"src/**/*.ts\" --fix"
  },
  "dependencies": {
    "fastify": "^3.27.2"
  }
}
